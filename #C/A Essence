캡슐화(Encapsulation) : 관련 있는 데이터와 함수를 하나로 묶는 것. 주로 함수를 public, 변수를 private으로 구분하는 것처럼 사용은 하되 내부적 접근은 불가능하게 한다.
구조체가 캡슐화가 불가능한 이유는 모든 데이터가 public이기 때문이다.

생성자(Constructor) : 객체의 생성과 동시에 한번 호출되는 함수. 최초 1회 초기화를 위해 사용하며 public이며 클래스와 이름이 같은 함수여야 한다.
