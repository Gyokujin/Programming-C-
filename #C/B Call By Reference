#include <iostream>
using namespace std;

// *참조자를 이용해서 다음 요구 사항에 부합하는 함수를 각각 작성하라.
// 1) 인자로 전달된 int형 변수의 값을 1씩 증가시키는 함수  2) 인자로 전달된 int형 변수의 부호를 바꾸는 함수
// 또한, 각 함수를 호출하여 결과를 확인할 수 있는 main함수까지 작성하라.
void AddOne(int &num)
{
    num++;
}

void Inverse(int &num)
{
    num *= -1;
}

int main()
{
    int num = 0;
    AddOne(num);
    cout << num << endl;

    AddOne(num);
    AddOne(num);
    AddOne(num);
    Inverse(num);
    cout << num << endl;
}
